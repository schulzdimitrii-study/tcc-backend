spring.application.name=tcc
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

jwt.secret=TccSuperSecretKeyForJwtTokenGeneration2026MustBeAtLeast256Bits
jwt.expiration=86400000

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/tcc}
spring.datasource.username=${DB_USER:schulzdimitrii}
spring.datasource.password=${DB_PASS:password}

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
